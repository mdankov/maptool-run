machine:
  environment:
dependencies:
  cache_directories:
    - ~/mapdir
  pre:
    - "[ -d ~/mapdir ] || mkdir ~/mapdir"
    - sudo apt-get update
    - sudo apt-get install cmake
    - bash build-map
test:
  post:
    - /bin/ture
